import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  inputContainer: {
    borderBottomWidth: 0,
    marginHorizontal: -15,
  },
  input: {
    color: '#fff',
    fontWeight: 'bold',
  },
  searchIcon: {
    marginRight: 10,
  },
});

export default styles;
