import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputContainer: {
    borderBottomWidth: 0,
    marginLeft: -15,
    marginRight: -15,
  },
  input: {
    color: '#fff',
    fontWeight: 'bold',
  },
  searchIcon: {
    marginRight: 10,
  },
});

export default styles;
